# 카드 배틀 로그라이크 — 시스템 명세서

> 대상: 신규 카드 전투 시스템(NewBattle) 기반 본 게임(SampleScene).
> 기준 문서: `기획서 0.6v`, `ONBOARDING.md`, FCM-RBFN `시스템_설계문서.md`.
> 용어: 구 "피버(Fever)"는 전부 **각성(Awaken)** 으로 통일.

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 장르 | 턴제 덱빌딩 로그라이크 (사신도) |
| 엔진 | Unity 6000.3.1f1 / C# |
| 핵심 루프 | 5장 손패로 카드 사용 → 게이지/상태 관리 → 속성 카드 10장 누적 시 **각성** → 3장 콤보 체이닝 |
| 차별점 | ① 조합으로 역할이 바뀌는 콤보 스킬 ② 카드 효과 + **입력 순서** 설계 ③ 적 공격과 플레이어 행동의 가치 |
| 데이터 | 카드/콤보 효과 전부 **Excel→JSON 데이터 드리븐**(하드코딩 X) |
| 적 AI | **FCM-RBFN** — 플레이어 덱 유형 분석 후 방해행동 선택 |

핵심 분리 원칙: 신규 시스템은 `NewBattleController.Instance != null` 가드로 레거시(`ElementSlotSystem`+`ComboSystem`)와 분리. 본 게임(SampleScene)은 **항상 신규 시스템** 사용.

---

## 2. 시스템 아키텍처

### 2.1 게임 흐름
```
GameStateController (스테이지 토글: 맵/전투/상점/휴식/이벤트)
   │  ShowMap → MapManager(분기형 맵)
   ▼
노드 선택 → Roundmanager.StartRound(RoundData)
   │  CombatRoundHandler / EliteRoundHandler / BossRoundHandler …
   ▼
전투: Roundmanager.StartCombat → 적 스폰 → NewBattleController.StartBattle
   │  (적 처치 반복 → 노드 종료)
   ▼
EndRound → NewBattleController.EndBattle → 보상(카드 획득) → 맵 복귀
```

### 2.2 컴포넌트 맵 (`Assets/Script/Battle/`)
| 영역 | 파일 | 역할 |
|---|---|---|
| 전투 코어 | `NewBattleController.cs` | 덱/드로우/각성/콤보 큐/방해/저주/연쇄/게이지/이펙트 트리거 |
| 덱 | `Deck/CardDeckSystem.cs` | 뽑을/패/버린/소멸 더미 관리 |
| 카드 효과 | `Deck/CardEffectResolver.cs` | **카드 데이터 드리븐 효과 엔진** |
| 콤보 | `ComboSkillDef.cs`, `Deck/ComboSkillDatabase.cs`, `Deck/ComboEffectResolver.cs` | 콤보 정의/DB/효과 엔진 |
| 카드 데이터 | `Card/CardData.cs`, `CardDatabase.cs`, `CardEffectData.cs`, `CardEnums.cs` | 카드 모델/JSON 로드 |
| 런 상태 | `Run/RunDeckState.cs` | **런 단위** 덱·영구공격력·AI 프로파일 보존 |
| 부트스트랩 | `NewBattleSystemBootstrap.cs` | SampleScene에서 신규 시스템 항상 활성화 |
| 유물 | `Relic/RelicDef.cs`, `RelicManager.cs` | 유물 데이터/보유 |
| 적 AI | `AI/EnemyAiModel.cs`, `EnemyDisruptionAI.cs`, `DisruptionLogger.cs` | 방해행동 추론/로깅 |
| UI | `UI/CardHandHUD.cs`, `NewCardView.cs`, `CardRewardUI.cs`, `RelicHUD.cs`, `CardEffectOverlay.cs`, `PlayerDamageOverlay.cs` | 손패/카드/보상/유물/이펙트 |

연동(수정된 레거시): `Round/Roundmanager.cs`(전투 구동·보상 분기·티어 골드), `Enemy/EnemyStat.cs`·`EnemyController.cs`(게이지·화상·강화·회복), `Player.cs`(상태·연쇄), `GameStateController.cs`(런 리셋).

---

## 3. 전투 시스템

### 3.1 덱 / 패 / 드로우
- 4더미 구조: 뽑을(draw) / 패(hand) / 버린(discard) / 소멸(exile).
- 패 한도 기본 **5장**(동적 증가 상한 10, 땅19/418).
- 전투 시작 시 5장 드로우. 카드 사용 = **더블클릭** 또는 위로 드래그.
- **D 드로우**: 패가 안 찼으면 1장 + **적 행동게이지 +1**. 패 만석 시 불가. (선택/픽커/연출 중 비활성)

### 3.2 카드 사용 흐름
1. 사용 요청 → 즉시 패에서 분리(`PullFromHand`) + 중앙 연출 시작(`_cardPresenting=true`).
2. 연출 종료 시 `ResolveCardUse` → 효과/게이지/각성 카운트 실행.
3. 게이지: 카드별 `gauge` 만큼 적 행동게이지 누적.
- 전투 종료(`EndBattle`) 시 진행 중 연출/지연효과 취소 → 보상과 겹침 방지.

### 3.3 카드 타입 (기획서 §3.2.2.1)
| 타입 | 정의 |
|---|---|
| 공격(Attack) | 적에게 직접 피해 |
| 스킬(Skill) | 직접 피해 없음(방어/제어/시너지) |
| 파워(Power) | 버린 더미로 안 가고 **이번 전투 지속 버프**. 공격/스킬 효과 처리 후 마지막에 처리 |

### 3.4 각성 (Awaken)
| 항목 | 값 / 동작 |
|---|---|
| 발동 | 무속성/파편 제외 **속성 카드 10장** 사용 시 즉시 (`AWAKEN_ACTIVATION_INPUT`, 바람326 등으로 가변) |
| 발동 시 | 10초 타이머, 적 게이지 정지, 모든 카드 효과→"드로우 1", 콤보 슬롯/스킬 활성, 파편·무속성 임시 격리, 패 가득 드로우 |
| 발동 중 | 슬라이딩 3장 콤보 매칭 → 큐 적재 + 시간 `COMBO_BONUS_SECONDS`(현재 0.1s) 추가(비급서 유물 +0.5s). 콤보 재사용: 발동 후 속성 입력 5회 |
| 종료 | 패/더미 스냅샷 복원 → 큐 콤보 일괄 발동(데미지 분산+셰이크) |
| 상수 | `AWAKEN_DURATION_SECONDS=10`, `COMBO_BONUS_SECONDS=0.1`, `COMBO_REUSE_INPUT=5` |

### 3.5 적 행동 게이지 (기획서 §적, per-enemy)
- `EnemyData.actionGaugeMax`(기본 20, **10~30 클램프**). 카드 게이지로 누적.
- **절반** 도달 → 방해행동(중간 패턴), **최대** 도달 → 적 공격 + 초기화(초과분 자연 이월).

### 3.6 방해행동 6종 (`TriggerDisruption`, AI가 선택)
| # | 행동 | 효과 |
|---|---|---|
| 0 | 저주 | 무작위 속성 저주 → 그 속성 카드 사용 시 플레이어 **6 피해**(2회 지속) |
| 1 | 탈진 | 무속성 더미 카드(500)를 뽑을 더미에 삽입 |
| 2 | 흡수 | 플레이어 각성 게이지 **-2** |
| 3 | 강화 | 적 다음 공격 1회 **+50%**(올림) |
| 4 | 회복 | 적 HP **30% 회복** + 쿨다운(다른 행동 2회 후 재사용) |
| 5 | 버리기 | 플레이어가 패에서 **2장 선택해 버림** |

---

## 4. 키워드 / 상태이상 (기획서 §3.2.2.1, 모든 스택 최대 999·int)

| 키워드 | 대상 | 동작 |
|---|---|---|
| 화상(burn) | 적 | 적 공격 시 피격 전 스택 n 고정피해 → 이후 n/2(내림) |
| 빙결(frost) | 적 | 카드 사용 시 행동게이지 상승을 스택만큼 상쇄(상쇄분 소모) |
| 연쇄(chain) | 플레이어 | 공격 카드 타격 시 **플레이어 공격력 10%** 추가타 → n-1, 타격수 반복 |
| 방어도(block) | 플레이어 | 적 피격 시 데미지 상쇄(상쇄분 소모), 초과분만 체력 |
| 저주(curse) | 플레이어 | 방해행동. 해당 속성 카드 사용 시 6 피해(2회) |
| 파편(fragment) | — | 이번 전투용 임시 카드(파편1~3) 생성. 소멸/방어/피해+드로우 |

---

## 5. 데이터 드리븐 카드/콤보 엔진

### 5.1 파이프라인
```
Card_DB_v2.xlsx / ComboSkill_DB.xlsx
   │  Tools > Card DB / Combo DB > Excel → JSON 변환
   ▼
Resources/CardDB/{Cards,CardEffects}.json
Resources/ComboDB/{ComboSkills,ComboEffects}.json
   │  CardDatabase / ComboSkillDatabase 로드
   ▼
CardEffectResolver / ComboEffectResolver (id로 효과 실행)
```

### 5.2 카드 ID 체계 (v2: 112장 / 효과 159건)
- FIRE 100~126, WATER 200~226, WIND 300~326, EARTH 400~426 (각 + 파워 12x~)
- NEUTRAL 500(방해 더미), FRAGMENT 501~503(파편)
- 등급: Normal/Rare/Epic (`CardData.rarity`)
- **기본 시작 덱(8장)**: 속성별 공격(게이지1/피해5) + 방어(게이지1/방어도3) = 100/101·200/201·300/301·400/401

### 5.3 효과 스키마 (CardEffects.json / ComboEffects.json 공통)
`When`(트리거) · `If`(조건) · `Do`(동사) · `Target` · `Amount`/`Formula` · `Hits`/`HitFormula` · `Status` · `CardFilter` · `From/ToZone` · `Select` · `Repeat` · `Extra`
- **Do**: DAMAGE, GAIN_BLOCK, HEAL, DRAW_CARD, DISCARD/EXHAUST_CARD, APPLY/GAIN_STATUS, GAIN_STAT(공격력, `Extra=Permanent=true`면 런 지속), ADD/MOVE/COPY_CARD, TRIGGER_HAND/DRAW_CARDS 등 (전종 구현)
- **When**: ON_USE 즉시 + 파워 등록형(ON_CARD_USED, ON_ELEMENT_CARD_USED …) + 생애주기형(ON_SELF_EXHAUST/DISCARDED …)

### 5.4 콤보 스킬
- 콤보 슬롯: 각성 중 속성 카드 입력을 **3장**까지 슬라이딩 기억(왼쪽부터, 가득차면 밀기).
- canonical **1000~1019(20종)** + alias 1020~1063(순서 변형, `RefComboID`로 효과 공유) = 총 64. **순서무관 매칭**.
- 보유 콤보: `NewBattleController.ownedComboRefIds`(빈 리스트=전체). 런타임 추가: `AddOwnedCombo(refId)` / 인스펙터 체크박스+"지금 적용". 최대 보유 5(기획).

---

## 6. 런(run) 진행

### 6.1 런 단위 상태 — `RunDeckState` (씬 영속 싱글톤)
- `RunDeck`(DeckEntry 리스트): 전투 간 덱 보존. 매 전투 `InstantiateForBattle()`로 새 CardInstance 생성(오염 방지).
- `PermanentAttackPower`: 땅410 등 영구 공격력(전투 시작 시 복원).
- AI 프로파일: 최근 방어 사용(f4), 각성 발동 수·비각성 사용 수(f7).
- `ResetRun()`: 새 런 시 기본값 복원 (`GameStateController.RestartRunToMap`에서 호출).

### 6.2 전투 보상 (기획서 §보상)
- **티어 골드**(전투 종료 시 일괄): 일반 30~50 / 정예 100~150 / 보스 200~250.
- **카드 획득**: 전투 후 **4장 제시 택1**(`CardRewardUI`, 런타임 생성) → `RunDeckState.AddCard`.
- (미구현) 정예/보스 콤보 보상·보스 유물 보상.

### 6.3 유물 — `RelicManager`
- 전투형/비전투형, 중복획득 X, 최대 99(기획). 현재 테스트 2종(이무기의 여의주=각성게이지 회복, 비급서=콤보 보너스 +0.5s).

### 6.4 맵 / 스테이지 (기획서 §스테이지 — 일부 미반영)
- 분기형 10라운드, 노드 타입: 전투/정예/보스/이벤트/휴식/상점.
- 휴식: 명상 = 최대체력 **20% 회복**.
- (미반영) 고정 층(1-2전투/5유물/10보스), 스테이지 2회 클리어=게임 클리어, NodeType.Relic, 상점/이벤트 개편.

---

## 7. 적 AI — FCM-RBFN (방해행동 선택)

랜덤 대신 **플레이어 덱 유형 + 전투 상황**으로 방해행동을 argmax 선택. 2단계: FCM(유형 군집) → RBFN(Q-Value). 오프라인 Python 학습 → 가중치를 `EnemyAiModel.cs`에 임베드 → 런타임 추론.

### 7.1 추론 흐름 (`EnemyDisruptionAI.Decide`)
```
1) 특성 x(6) = [f1 화상, f2 파편, f3 연쇄, f4 방어, f5 코스트, f7 피버] ← RunDeck/플레이 분석
2) 컨텍스트 c(8) = [적HP, 플HP, 손패 화상/파편/연쇄/방어/무속성 비율, 손패수/10]
3) μ(5) = 마할라노비스 퍼지 소속도(화상/파편/연속타격/피버/안정형)
4) Q(a) = Σμ·QTypeWeights + Σc·QContextWeights + QBias   (a = 6 방해행동)
5) 가용성 마스크(버리기 손패<2 제외 / 회복 쿨다운 제외) → argmax
```
- 카드 분류: **키워드(효과) 기준**(화상=burn효과, 연쇄=chain/다타격, 방어=block/heal, 파편=fragment+땅원소).
- 토글: `useAiDisruption`(기본 ON, 폴백 랜덤), `aiExplorationEpsilon`(탐험), `logAiDecisions`(CSV 로깅).

### 7.2 학습 데이터화 (`DisruptionLogger`)
- `persistentDataPath/ai_logs/run_*.csv` 스키마: `f1..f7, enemy_hp, player_hp, hand_*, hand_count, recover_ready, action`.
- 수집 → Python(`fcm_retrain_gk.py`/`rbfn_train_gk.py`) 재학습 → 산출 .cs를 `EnemyAiModel.cs`에 교체.
- ⚠️ 현재 가중치 = **합성데이터 기반 플레이스홀더**. 실데이터 + 결과기반 보상으로 고도화 필요.

---

## 8. UI 시스템
- `CardHandHUD`: 손패(부채꼴) + 콤보 슬롯/스킬 + 각성 게이지/히스토리 + 카드 선택/픽커 + 더미 뷰어 + 카드 사용 연출. (cardPrefab=NewSkillCard 등 **Inspector 배선 의존**)
- `NewCardView`: 카드 표시 + 드래그/hover(앞으로)/클릭.
- `CardRewardUI`/`RelicHUD`: 런타임 생성.
- 각성 게이지: `Player.hpBar`에 앵커, 미배선 시 화면 상단 중앙 fallback(`awakenGaugeFallbackPos`).

---

## 9. 씬 구성 (SampleScene)

| 직접 배치(필수) | 코드 자동 생성 |
|---|---|
| GameStateController, Roundmanager, MapManager, CombatStageController, MoneyManager | NewBattleController·RelicManager·RelicHUD·RunDeckState (부트스트랩) |
| **NewBattleSystemBootstrap**(부트스트랩 본체) | CardEffectOverlay, PlayerDamageOverlay, EventSystem (StartBattle) |
| Canvas/CombatStage, **배선된 CardHandHUD** | PlayerLogic(Player), 적, CardRewardUI (Roundmanager) |

- 적 AI 클래스(EnemyAiModel/EnemyDisruptionAI/DisruptionLogger)는 **static** — 오브젝트 없음.
- 부트스트랩이 레거시(ElementSlotSystem/ComboSystem/ElementSlotHUD/CardSystem) 비활성화.

---

## 10. 주요 상수 요약
| 상수 | 값 | 위치 |
|---|---|---|
| 패 한도 / 상한 | 5 / 10 | CardDeckSystem |
| 시작 드로우 | 5 | NewBattleController.START_DRAW |
| 각성 발동 입력 | 10 | AWAKEN_ACTIVATION_INPUT |
| 각성 지속 | 10s | AWAKEN_DURATION_SECONDS |
| 콤보 보너스 | 0.1s | COMBO_BONUS_SECONDS |
| 콤보 재사용 입력 | 5 | COMBO_REUSE_INPUT |
| 저주 피해 / 지속 | 6 / 2회 | CURSE_PLAYER_DAMAGE / CURSE_DURATION_USES |
| 적 게이지 max | 10~30 | EnemyData.actionGaugeMax |
| 스택 상한 | 999 | (전 상태) |
| 연쇄 피해 | 공격력 10% | CardEffectResolver |

---

## 11. 알려진 한계 / TODO
- **콤보·유물 보상 UI**(정예/보스/보스) 미구현 — 현재 콤보는 인스펙터 수동 추가.
- **맵 개편**(고정 층/스테이지 2회 클리어/유물 라운드/상점·이벤트) 미반영.
- 적 AI 가중치 **실데이터 재학습** 필요(현재 합성).
- 단일 적 구조(ALL/RANDOM_ENEMY → 현재 적 1체).
- UI: 부채꼴 손패에서 가려진 카드 선택 어려움(겹침) — 검토 중.
- 일부 레거시 파일 EUC-KR 잔재(주석) — 편집 시 UTF-8 유지.

---

## 12. 디버그
- `NewBattleController.logVerbose` ON → 카드/각성/콤보/저주/연쇄/유물/**적AI(μ·선택)** 로그.
- `[적AI] μ=[..] → 선택=X`, `[RunDeck] 카드/영구공격력 …`, `[방해] …`, `[콤보 스킬] …`.
